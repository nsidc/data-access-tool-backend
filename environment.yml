name: dat-backend
channels:
  - conda-forge
  - nsidc
  - nodefaults
dependencies:
  - python ~=3.12.0
  - pydantic ~=2.7.3
  - flask ~=2.3.3
  - flask-restx ~=1.3.0
  - gunicorn ~=22.0
  - requests ~=2.32
  - openssl ~=3.4.1 # required for production ssl behind the Apache proxy.

  # Dev deps
  - pytest ~=8.2
  - pyopenssl # required for test server w/ ssl
  - pre-commit ~=4.1
  - mypy ~=1.15
  - bump-my-version ~=1.1
